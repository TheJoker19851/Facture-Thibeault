/**
 * @deprecated The application data model moved to Firebase. This helper remains
 * only so the original starter files do not silently become a second backend.
 */
export function getDb(): never {
  throw new Error("D1 is legacy scaffolding; use Firebase queries and Cloud Functions.");
}
